import { guest_list } from '../14.Exercises/Gapp'

console.log('Print a message the number of people you are inviting to dinner.')

console.log(`So we are invited tonight ${guest_list.length} \n Thank you`);